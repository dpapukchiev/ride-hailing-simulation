---
description: Keep SPEC.md in sync with code changes
alwaysApply: true
---

# SPEC.md Sync

- Update `SPEC.md` whenever code or spec changes alter behavior, structure,
  dependencies, or workflows.
- If a change is not reflected in the spec, add or adjust a section in the
  same PR/commit.
- Keep the spec concise, factual, and aligned with the current codebase.
