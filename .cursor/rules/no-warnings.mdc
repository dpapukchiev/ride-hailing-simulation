---
description: Ensure code compiles without warnings
alwaysApply: true
---

# No Warnings Policy

- Code must compile without any warnings when running `cargo check`, `cargo build`, or `cargo test`.
- Fix all compiler warnings before committing code.
- Common warnings to address:
  - Unused variables or imports
  - Unnecessary `mut` keywords
  - Dead code
  - Unused comparison warnings
- Run `cargo check` or `cargo test` to verify no warnings before finalizing changes.
- If a warning cannot be fixed (e.g., it's a false positive), use `#[allow(warning_name)]` with a comment explaining why, but prefer fixing the underlying issue.
